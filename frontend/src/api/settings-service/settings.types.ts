import { Settings, PostSettings } from "#/types/settings";

// ApiSettings is now just an alias for the main Settings type
export type ApiSettings = Settings;

// PostApiSettings is now just an alias for PostSettings
export type PostApiSettings = PostSettings;
